---
import { cn } from "@/lib/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<div
  class={cn(
    "flex flex-col gap-y-8 p-4 md:p-6 lg:p-8 w-full transition-[padding]",
    className,
  )}
>
  <slot />
</div>
