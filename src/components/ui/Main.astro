---
import { cn } from "@/lib/utils";
import { ScrollArea } from "./scroll-area";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<ScrollArea
  as="main"
  className={cn(
    "w-full flex flex-col gap-y-4 lg:gap-y-8 h-dvh overflow-y-auto",
    className
  )}
>
  <slot />
</ScrollArea>
