---
import { cn } from "@/lib/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<div class={cn("flex items-center justify-end gap-2", className)}>
  <slot />
</div>
