---
import { cn } from "@/lib/utils";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<article
  class={cn(
    "p-5 md:p-6 lg:p-8 rounded-3xl border-2 border-border hover:border-foreground/25 shadow-xl shadow-transparent hover:shadow-foreground/5 transition-[border,box-shadow,padding,height,width] flex flex-col w-full gap-4 bg-linear-to-br from-box-foreground/15 from-5% to-box",
    className
  )}
>
  <slot />
</article>
