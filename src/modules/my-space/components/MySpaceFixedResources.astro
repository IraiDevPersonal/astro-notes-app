---
import FolderCard from "@/modules/folder/components/FolderCard.astro";
---

<div
  class="grid grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-2 md:gap-4 transition-[gap]"
>
  {
    Array.from({ length: 17 }).map(() => (
      <FolderCard>tarjeta de recurso</FolderCard>
    ))
  }
</div>
